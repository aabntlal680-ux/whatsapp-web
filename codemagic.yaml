workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    scripts:
      - name: Install pnpm and Dependencies
        script: |
          # 1. تثبيت pnpm وتنظيف الكاش والمجلدات القديمة تماماً لمنع خطأ الـ HOIST DIFF
          npm install -g pnpm --force
          rm -rf node_modules .pnpm-store pnpm-lock.yaml
          
          # 2. التثبيت النظيف بالهيكلية المسطحة المناسبة لأندرويد
          pnpm install --no-frozen-lockfile --shamefully-hoist
          
          # 3. تثبيت إضافة الـ gradle-plugin في جذر الـ Workspace بشكل صحيح باستخدام خيار -w
          pnpm add expo-module-gradle-plugin --save-dev -w
      - name: Build APK
        script: |
          # 4. تجهيز متغيرات البيئة ومسار نود لـ Expo
          NODE_PATH=$(which node)
          export NODE_EXECUTABLE_PATH=$NODE_PATH
          
          # 5. تنظيف وبناء مجلد أندرويد من الصفر
          rm -rf android
          npx expo prebuild --platform android --no-install
          
          # 6. ربط مجلد الإضافة بملف الإعدادات
          echo "includeBuild('node_modules/expo-module-gradle-plugin')" >> android/settings.gradle

          # 7. قص السطر 85 التالف من ملف إكسبو الداخلي لمنع تعليقة الـ release property
          TARGET_FILE=$(find node_modules -name "ExpoModulesCorePlugin.gradle" | head -n 1)
          if [ -f "$TARGET_FILE" ]; then
            echo "Fixing Expo Component issue in: $TARGET_FILE"
            sed -i '' '/withVariants/d' "$TARGET_FILE" || sed -i '/withVariants/d' "$TARGET_FILE"
          fi
          
          # 8. تشغيل البناء والتجميع النهائي المستقر
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon -PnodeExecutable=$NODE_PATH

    artifacts:
      - android/app/build/outputs/apk/release/*.apk
